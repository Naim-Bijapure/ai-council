{
    "appKey": "qwen",
    "_comment": "Qwen.ai selector config. Input is a textarea. Send button is a div. Response and completion selectors are best guesses—please inspect the live DOM during an active chat to refine them. The stop button may appear as the send button with a different icon/class, but we provide a generic fallback.",
    "selectors": {
        "input": [
            "textarea.message-input-textarea",
            "textarea[placeholder*='How can I help you t']",
            "div.message-input"
        ],
        "send": [
            "div.message-input-right-button-send",
            "div.message-input-right-button",
            "button#show-shortcuts-button"
        ],
        "response": [
            "div.desktop-layout-content",
            "div.chat-message-input-fixed-container",
            "div.message-input-container"
        ],
        "completion": ["button[aria-label='Stop']", "div.message-input-right-button-send.stop", "div[class*='stop']"],
        "blocked": ["div[class*='rate-limit']", "div[class*='captcha']", "div[class*='error']"],
        "loginError": ["a[href*='login']", "a[href*='signin']", "button[class*='login']"]
    }
}
