# Copy to .env to override defaults.

# Show developer tools (Run Diagnostics + Selector Probe) in the side panel.
#   true  = always visible
#   false = always hidden
# When unset, defaults to dev-only (visible in `wxt dev`, hidden in production builds).
WXT_SHOW_DEV_TOOLS=true
